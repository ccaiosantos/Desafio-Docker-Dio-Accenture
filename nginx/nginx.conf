events {}

http {
    upstream all {
        server web1:80;
        server web2:80;
        server web3:80;
    }

    server {
        listen 4500;

        location / {
            proxy_pass http://all/;
        }
    }
}